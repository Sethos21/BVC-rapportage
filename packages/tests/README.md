# @bvc/tests

Nog leeg. Unit tests staan nu bij de broncode in `packages/domain` en
`packages/data-contracts`. Deze package is gereserveerd voor
integratie-/broncontract-/reconciliatie-/snapshot-/end-to-endtests die een
draaiende database nodig hebben (09_ACCEPTATIE_EN_TESTPLAN_v0.3.md), zodra
er een gekozen testomgeving is (zie root-README, open punt: hosting).
