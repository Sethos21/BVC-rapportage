{
  "name": "bvc-rapportage",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --filter=./packages/** --filter=./apps/** build",
    "typecheck": "pnpm -r typecheck",
    "lint": "oxlint .",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "oxlint": "^1.71.0",
    "typescript": "^5.9.3"
  }
}
